#include<stdio.h>
#include<string.h>
#define SIZE 100
int main()
{
    char s_length,g_length;
    int i
    scanf("%s",&s_length);
    scanf("%s",&g_length);
    for(i=0;i<n;i++)
    {
        if()
    }
}
